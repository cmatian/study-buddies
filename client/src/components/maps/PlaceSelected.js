import React from 'react';

// display currently selected place with detail information
class PlaceSelected extends React.Component {
    render() {
        return(
            <div>PlaceSelected</div>
        );
    }
}
export default PlaceSelected;